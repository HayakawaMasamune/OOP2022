<Window x:Class="CollarChecker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CollarChecker"
        mc:Ignorable="d"
        Title="MainWindow" Height="300" Width="500">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column ="0">
            <Label Name="Color" Grid.Row="0" />
        </Grid>

            <Grid Grid.Column ="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Grid.Row ="0" Text="R" HorizontalAlignment="Center"/>
            <Slider x:Name="RedSlider" Grid.Column ="1" Maximum="255"/>
            <TextBox Grid.Column="2" Grid.Row ="0" Text ="{Binding ElementName=RedSlider,Path=Value,UpdateSourceTrigger=PropertyChanged}"/>
            
            <TextBlock Grid.Column="0" Grid.Row ="2" Text="G" HorizontalAlignment="Center"/>
            <Slider x:Name="GreenSlider" Grid.Column ="1" Grid.Row ="2" Maximum="255"/>
            <TextBox Grid.Column="2" Grid.Row ="2" Text ="{Binding ElementName=GreenSlider,Path=Value,UpdateSourceTrigger=PropertyChanged}"/>
            
            <TextBlock Grid.Column="0" Grid.Row ="4" Text="B" HorizontalAlignment="Center"/>
            <Slider x:Name="BlueSlider" Grid.Column ="1" Grid.Row ="4" Maximum="255"/>
            <TextBox Grid.Column="2" Grid.Row ="4" Text ="{Binding ElementName=BlueSlider,Path=Value,UpdateSourceTrigger=PropertyChanged}"/>
            
        </Grid>
    </Grid>
</Window>
